import React, { Component } from 'react';

class Terminal extends Component {
  render() {
    return(
      <section className="grid-x terminal__body">
        <div className="cell large-8">This is where the terminal will be</div>
      </section>
    );
  }
}

export default Terminal;